apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ingest-relay.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "ingest-relay.name" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ingest-relay.name" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "ingest-relay.name" . }}
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["ingest-relay", "serve", "--host", "0.0.0.0", "--port", "8080"]
          ports:
            - name: http
              containerPort: 8080
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- range $key, $_ := .Values.secrets }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "ingest-relay.fullname" $ }}
                  key: {{ $key }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources.api | nindent 12 }}
