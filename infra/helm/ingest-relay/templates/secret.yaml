{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ingest-relay.fullname" . }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
