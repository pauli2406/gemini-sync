id: studio-managed-secret-resolution
name: Managed secret create and resolution
critical: true
pytest_selector: tests/test_managed_secrets_registry.py::test_managed_secret_create_and_list_and_resolve
acceptance:
  - Managed secret can be stored and listed without exposing value.
  - Secret value resolves from managed store for runtime usage.
