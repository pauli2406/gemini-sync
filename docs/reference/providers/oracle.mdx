---
title: "Provider: Oracle"
---

Use `source.type: oracle` for SQL pull connectors.

Service name DSN:

```text
oracle+oracledb://<user>:<password>@<host>:<port>/?service_name=<service_name>
```

SID DSN:

```text
oracle+oracledb://<user>:<password>@<host>:<port>/?sid=<sid>
```
