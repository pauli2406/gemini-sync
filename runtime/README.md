# Runtime

Runtime implementation lives in the Python package:

- `ingest_relay/`

The package includes:

- connector execution pipeline
- push API service
- state management models
- object storage publisher
- Gemini ingestion client
